#include <stdio.h>
#include <unistd.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	int a = 3;
	int b = 5;
	int sum = a + b;
	pid_t pid;

	pid = getpid();

	printf("Sum is %d\n", sum);
	printf("Pid is %u\n", pid);

	return (0);
}
